services:
  mcp:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/workspaces/garage61-race-engineer-mcp
    env_file:
      - .env
    # depends_on:
    #   postgres:
    #     condition: service_healthy

  # postgres:
  #   image: postgres:17-alpine
  #   environment:
  #     POSTGRES_USER: garage61
  #     POSTGRES_PASSWORD: garage61
  #     POSTGRES_DB: garage61
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U garage61"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

# volumes:
#   postgres_data:
